before_install: sudo apt-get install cmake

language: cpp

compiler:
  - gcc
  - clang

before_script:

   - mkdir build
   - cd build


script: 

 - cmake ../

notifications:
  recipients:
    - mdonaghey075@gmail.com
  email:
    on_success: change
    on_failure: always